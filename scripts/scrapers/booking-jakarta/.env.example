# ClickHouse (Dispar Lakehouse)
CH_URL=http://192.168.18.187:18123
CH_USER=dispar
CH_PASSWORD=<ambil-dari-portainer-stack-6-env>
CH_DATABASE=serving

# Scraper behavior (safeguards)
SCRAPE_MAX_PAGES=4              # max search-result pages to paginate
SCRAPE_DELAY_MS=4000            # polite delay between requests (ms)
SCRAPE_DETAIL_DELAY_MS=6000     # extra polite delay before hitting hotel detail
SCRAPE_MAX_HOTELS=60            # hard cap on detail-page hits per run

# Booking.com search params (override as needed)
CHECKIN_OFFSET_DAYS=14          # check-in = today + N days
NIGHTS=1                        # length of stay
ADULTS=2
